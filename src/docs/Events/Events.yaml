paths:
  /api/v1/events/:
    get:
      summary: Obtiene todos los eventos
      tags:
        - Events
      responses:
        "200":
          description: Los eventos se obtuvieron exitosamente de la base de datos
        "500":
          description: Error interno del servidor (query incorrecta o falla de conexion con la base de datos)